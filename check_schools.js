const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const schools = await prisma.school.findMany({select: {id: true, subdomain: true, name: true}}); console.log(JSON.stringify(schools, null, 2)); } main().catch(e => console.error(e)).finally(() => prisma.$disconnect());